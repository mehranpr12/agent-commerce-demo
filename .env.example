# Two separate private keys — one per agent
PROVIDER_PRIVATE_KEY=0x
CONSUMER_PRIVATE_KEY=0x

# Chain (both agents must be on the same chain)
AZETH_CHAIN=baseSepolia

# Optional: Custom RPC
# AZETH_RPC_URL=
